import { GetResourceResponse } from './get-resource-response';

describe('GetResourceResponse', () => {
  it('should create an instance', () => {
    expect(new GetResourceResponse()).toBeTruthy();
  });
});
